export const selectCats = ({ cat }) => cat.cats;
export const selectCurrentPage = ({ cat }) => cat.currentPage;
export const selectTotalCats = ({ cat }) => cat.totalCats;
export const selectPerPage = ({ cat }) => cat.perPage;